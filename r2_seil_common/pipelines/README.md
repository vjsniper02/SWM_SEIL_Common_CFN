## glue-local-docker-build.yaml
Azure DevOps pipeline to create Glue local test docker image and push to AWS ECR.

## glue-test-build-deploy.yaml
Azure DevOps pipeline to create a Glue job. It runs unit test, push code to S3 bucket and create Glue job.

